# Testing with the <time.h> library in C

clock.c shows the internal time of the computer while timer.c let's the user choose a number of minutes to start a countdown

To start clock.c

```
$ ./clock
```

To start timer.c

```
$ ./timer (int arg) // Where arg is a number of minutes
```
